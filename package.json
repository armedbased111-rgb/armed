{
  "name": "arm-stack",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w @arm/types build && npm run -w @arm/config build",
    "dev:web": "npm run -w @arm/web dev",
    "dev:api": "npm run -w @arm/api dev"
  }
}
